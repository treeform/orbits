# Package

version       = "0.1.0"
author        = "treeform"
description   = "Orbits - orbital mechanics library for nim."
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 1.0.0"
requires "chroma >= 0.0.1"
requires "pixie >= 1.1.2"
requires "vmath >= 1.0.1"
requires "print >= 0.1.0"
requires "cairo >= 1.1.0"
